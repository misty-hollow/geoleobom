# 공통 작업 규칙 (AGENTS.md)

이 저장소의 기본 원칙은 **자율 실행 + 명확한 상한선**이다.
제품 의미와 안전 경계를 지키는 한, 에이전트는 구현 방법·파일 구조·검사 순서를 스스로 정한다.

기본 흐름:

```text
의도 확인 -> 구현 -> 관련 회귀검사 -> 실제 흐름 확인 -> 완료
```

사용자가 만들기·수정·고치기를 요청하면 구현을 시작한다. 별도 계획 승인, RFC, 작업 카드,
증거 패키지, 전수조사, 리뷰 라운드는 기본값이 아니다. 실제 위험이나 사용자 요청이 있을 때만 추가한다.

## 1. 권위와 기준

권위 순서는 다음과 같다.

1. 현재 사용자의 명시적 지시와 확정된 제품 의도
2. `PROJECT.md`가 가리키는 현재 확정설계
3. 사용자에게 보이는 UI/UX는 `web/DESIGN.md`
4. 이 `AGENTS.md`와 저장소의 적용 가능한 작업 규칙
5. 현재 코드와 실제 런타임 동작
6. 테스트·리뷰·과거 계획·픽스처·관례

낮은 권위가 높은 권위를 재정의하지 않는다. 사용자가 제품 결정을 명확히 바꿨다면 그 결정이 우선하며,
필요한 경우 현재 확정설계를 함께 갱신한다. 오래된 테스트나 문서가 새 승인과 충돌하면 구현을 멈추는 대신
그 산출물을 맞게 고친다.

최신 `origin/main`과 실제 저장소 상태를 과거 첨부본·대화 요약보다 우선한다.

## 2. 바로 구현하되, 범위를 넓히지 않는다

- 요청된 결과를 완성하는 **가장 작은 완전한 변경**을 만든다.
- 승인된 제품 범위 안의 컴포넌트 분리, 함수 구조, 내부 타입, 일반 개발 의존성 선택은 에이전트가 정한다.
- 한 가지 요청을 계획·문서·리뷰 절차 때문에 여러 작업으로 인위적으로 쪼개지 않는다.
- 새 범용 인프라, 호환 계층, 이중 계약, 마이그레이션, rollout 장치는 실제 배포 소비자나 영속 데이터가 요구하지 않으면 만들지 않는다.
- 리뷰에서 실제 결함이 발견되면 별도 계획 단계로 돌아가지 말고 고치고 다시 확인한다.
- 리뷰어·서브에이전트는 사용자가 정한 작업 단계를 바꿀 수 없다.

## 3. 멈춰야 하는 경우

다음 경우에만 필요한 부분을 멈추고 사용자 결정을 받는다.

- 제품 의미·계산 의미·API 의미·저장 데이터 의미를 바꿔야 하는데 사용자의 결정이 없는 경우
- 새 외부 서비스·지속 비용·민감 권한·보안 경계를 추가해야 하는 경우
- 개인정보, 데이터 손상, 금전, 인증·권한, 배포, 복구 불가능한 작업에 구체적 위험이 있는 경우
- 되돌릴 수 없는 삭제나 운영 상태 변경이 필요한 경우

그 밖의 구현 세부사항 때문에 승인을 반복 요청하지 않는다. 설계에 빈칸이 있어도 독립적으로 가능한 부분은 계속 진행한다.

## 4. 걸어봄의 불변 경계

현재 확정설계가 바뀌지 않는 한 다음을 보존한다.

- 좌표 계약과 계산 모델, canonical snap, 시간 모델, 비교 규칙, 버전 규약
- 검색·분석·경로·비교의 공개 API 의미
- 사용자 좌표 원문과 검색어를 로그에 남기지 않는 개인정보 규칙
- 비밀값을 저장소·채팅·로그에 노출하지 않는 규칙
- Kakao REST 키는 서버 전용, 브라우저 키는 허용된 프론트 설정 경계 안에서만 사용
- 큰 GeoPackage·OSRM·PBF 산출물을 Git에 넣지 않는다
- 새 비용·새 서비스·스택 변경은 사용자 승인 없이 도입하지 않는다

사용자가 위 제품 경계를 명시적으로 변경한 경우, 그 승인 범위에서는 현재 확정설계를 먼저 또는 같은 일관된 변경 안에서 갱신하고 구현한다. 이미 승인된 결정을 다시 승인받지 않는다.

## 5. 검증

기본 검증은 **변경과 직접 관련된 가장 좁은 회귀검사 + 적용 가능한 lint/type/build + 대표 실제 흐름 1회**다.

- 실제 버그를 고쳤다면 가능한 경우 그 버그를 재현하는 회귀검사를 추가한다.
- 변경과 무관한 전체 테스트 행렬·기기 팜·부하검사·전수 검증은 기본값이 아니다.
- 기존 PR CI가 실행되면 그 품질 게이트는 그대로 적용한다.
- 좌표·시간·캐시·데이터 변환·API 계약·배포·보안처럼 실패 비용이 큰 변경은 필요한 범위까지 검증을 넓힌다.
- 모의 OSRM·합성 데이터와 실제 OSRM·실데이터 검증을 구분한다. 실행하지 않은 검사는 통과했다고 쓰지 않는다.
- 픽스처는 실제 시스템이 만들 수 있는 상태를 모델링한다. 테스트를 통과시키기 위해 불가능한 조합을 만들지 않는다.
- 중요한 상태 변경은 가능하면 authoritative readback 또는 실제 사용자/운영자 흐름으로 확인한다.

독립 검토는 계산·API 계약·데이터 변환·보안/개인정보·배포/복구 등 **실제 위험을 유의미하게 낮출 때** 사용한다.
루틴 UI·문구·내부 리팩터링에 자동으로 붙이지 않는다. 검토 후 수정이 생기면 위험한 변경분만 다시 볼 수 있으며, 사소한 수정 때문에 전체 리뷰를 처음부터 반복하지 않는다.

## 6. Git과 작업공간

- 작업 시작 전에 현재 브랜치, 저장하지 않은 변경, 열린 관련 PR을 확인한다.
- 다른 사람이 만든 변경을 덮어쓰거나 정리하지 않는다.
- 같은 파일·같은 작업트리를 두 에이전트가 동시에 수정하지 않는다. 실제 충돌이 있을 때만 별도 worktree를 쓴다.
- 구현 요청에는 필요한 branch/commit/push/Draft PR까지를 정상 완료 흐름으로 볼 수 있다. 사용자가 원치 않으면 만들지 않는다.
- `main` 직접 push, force push, 보호 규칙 우회는 하지 않는다.
- **Ready 전환, merge, release, production deploy, 원격 브랜치 삭제, destructive operation은 사용자의 명시적 지시 없이 하지 않는다.**
- 운영 배포를 요청받은 경우 검증된 main 커밋, 배포 후 실제 동작 확인, 복구 경로를 보존한다.

## 7. 개발 환경

사용자 PC에서는 `PROJECT.md`의 개발환경 결정을 따른다.

- 앱 코드 개발의 Git·Node·Python은 Windows 환경을 사용한다.
- WSL2는 현재 Docker 엔진 용도이며 Windows/WSL의 Git·Node·Python을 섞지 않는다.
- 텍스트 파일은 저장소에서 LF를 유지한다.
- CI·독립 검토 환경의 Linux 실행은 사용자 PC 환경을 바꾼 것으로 보지 않는다.

## 8. 문서와 보고

문서는 제품·운영 사실을 보존하기 위해 필요할 때 갱신한다. 루틴 구현마다 별도 계획서·증거 문서·상태 장부를 만들지 않는다.

- 현재 확정설계: 제품·계산·API 의미가 바뀔 때
- `web/DESIGN.md`: 사용자에게 보이는 확정 UI/UX 규칙이 바뀔 때
- `STATUS.md`: 운영 변화, 중요한 막힘, 인계, 주간 상태가 바뀔 때
- README: 실행·검사·배포·복구 방법이 실제로 바뀔 때

보고는 짧게 한다.

1. 무엇이 바뀌었는지
2. 무엇을 실제로 확인했는지
3. 남은 실제 위험이나 미확인 사항

실행하지 않은 검사, 존재하지 않는 증거, 확인하지 않은 운영 상태를 완료로 보고하지 않는다.
