# 공통 작업 규칙 (AGENTS.md)

Claude Code와 Codex가 공통으로 따르는 규칙이다. **규칙은 이 파일 한 곳에서만 관리한다.**
근거: 개발 운영 가이드 3·4·6·7절.

## 1. 작업 시작 전

1. `PROJECT.md`와 `STATUS.md`를 읽는다.
2. 이번 작업에 해당하는 v2.2 절을 읽는다 (`docs/걸어봄_확정설계_v2.2.md`). PROJECT.md 2절이 어디를 볼지 안내한다.
3. 현재 브랜치와 저장하지 않은 변경을 확인하고, 기존 작업을 지우지 않는다.
4. **진행 중인 구현 작업은 하나만** 수행한다.

## 2. 작업 카드 (구현 전에 합의할 여섯 줄)

> 목표: 사용자가 하게 될 행동 또는 계산 하나
> 기준: v2.2의 관련 절
> 완료 조건: 사람이 확인할 결과 2~3개
> 범위: 이번에 바꿀 부분 / 연결만 하고 남겨둘 부분
> 증거: 화면, 실제 응답, 검사 출력 중 필요한 것
> 미완료 조건: 무엇이 남으면 완료로 표시하지 않을지

구현 계획은 5줄 안팎으로 먼저 제시한다. 범위를 승인받은 뒤 그 안에서 구현·실행·관련 오류 수정까지 이어서 수행한다.

## 3. 하지 않는 것

- 요청과 관련 없는 정리, 리팩터링, 패키지 일괄 업그레이드.
- 새 서비스·새 데이터베이스·신생 라이브러리 추가.
- 테스트를 통과시키려고 기대값을 바꾸거나 검사를 끄는 것.
- v2.2에 없는 API 필드명·에러 코드 추가.
- `.env`나 비밀키를 저장소·채팅·로그에 넣는 것.
- 사용자 좌표 원문·검색어를 로그에 남기는 것 (v2.2 5절).
- 거대한 GeoPackage·OSRM·PBF 파일을 저장소에 넣는 것.
- 되돌릴 지점을 만들기 전에 전체 초기화·강제 덮어쓰기를 실행하는 것.

## 4. 별도 승인이 필요한 것

아래는 진행 전에 **대상과 복구 방법**을 제시하고 승인받는다.

- API 계약·에러 코드·계산 의미·저장 데이터·기술 스택 변경 (문서를 먼저 고쳐야 한다)
- 외부 배포, 운영 서버 작업
- 파일·데이터 삭제, 실데이터 교체
- 새 비용이 발생하는 작업
- 제품 코드 디렉터리 구조를 새로 만드는 것

## 5. 사실과 추측을 구분한다

- **실제 실행한 검사**와 **읽기만 한 검토**를 구분해 보고한다.
- **실제 결과**와 **모의(mock) 결과**를 구분한다. 모의 OSRM 응답으로 통과한 것은 실제 OSRM 연결 성공이 아니다.
- 도구 제한으로 실행하지 못한 검사는 **성공으로 계산하지 않고 미확인으로 보고**한다.
- 설계에 빈칸이나 모순이 있으면 추측해서 채우지 않는다. 해당 문장, 필요한 행동, 가장 작은 수정안을 제시하고 멈춘다.

## 6. 완료 보고 형식

1. 무엇이 달라졌는가 (세 줄 안팎)
2. 실제 실행한 검사와 그 결과
3. 미확인 사항
4. 사람이 따라 할 확인 순서

## 7. 막혔을 때

같은 실패가 두 번 반복되면 수정을 멈추고 아래 순서로 전환한다 (가이드 8절).

1. 현재 상태 보존 — 브랜치·변경 목록·실패 로그 기록. 관계없는 업그레이드·재설치 중단.
2. 재현 입력 고정 — 한 가지 사례로 좁히고 기대 결과와 실제 결과를 나란히 적는다.
3. 경계를 하나씩 확인 — 브라우저 → API → 데이터 조회 → OSRM 순서로 어디까지 성공했는지 본다.
4. 가설 하나만 검사 — 가장 유력한 원인 하나와 그것이 맞을 때 관찰될 결과를 적고 최소 확인을 수행한다.
5. 최소 수정 후 재검사 — 원래 실패 사례와 관련 기존 동작을 함께 확인한다.

## 8. 이 PC의 개발 환경

승인된 환경 결정은 `PROJECT.md` 3절에 있다. 요약하면 다음과 같다.

- 개발 도구는 **Windows에 통일**한다. WSL 안의 Git·Node·Python을 번갈아 쓰지 않는다.
- 기본 셸은 **PowerShell**이다.
- 파일은 **UTF-8(BOM 없음)**으로 저장하고, 저장소의 텍스트 파일 줄바꿈은 `.gitattributes`가 **LF**로 고정한다.
- 환경 결정을 바꾸려면 `PROJECT.md` 3절을 고치고 `STATUS.md`에 기록한다.

## 9. 문서 갱신

- 작업이 끝나면 `STATUS.md`를 갱신한다. 현재 상태와 **다음 작업 하나**만 남긴다.
- `README.md`에는 **실제 실행해 확인한 명령만** 적는다. 확인하지 못한 것은 `미확인`으로 표시한다.
- v2.2 원문을 다른 파일에 복제하지 않는다.
