# 공통 작업 규칙 (AGENTS.md)

Claude Code와 Codex의 현재 작업 규칙은 이 파일 한 곳에서 관리한다.
2026-09-11 사용자 승인: 반복 승인을 줄이고 검사·독립 검토·복구 조건에 따라 자동화한다.
개발 운영 가이드 v1의 승인·보고·갱신 절차와 충돌하면 이 파일을 따른다.
제품·계산·API·서버 규약은 **PROJECT.md 1절이 지정하는 현재 확정설계**가 기준이다. 버전이 올라가도 이 파일을 고치지 않는다.

## 1. 시작과 작업 크기

1. PROJECT.md, STATUS.md, 이번 작업에 해당하는 현재 확정설계 절을 읽는다.
2. 브랜치·저장하지 않은 변경·열린 PR을 확인한다. 다른 작업을 지우거나 덮어쓰지 않는다.
3. 진행 중인 구현 작업은 하나만 둔다. 최신 origin/main에서 작업 브랜치를 만든다.
4. 승인된 목표 안의 편집·검사·일반 오류 수정·커밋·push·PR 생성은 이어서 실행한다.
5. 반나절을 넘는 작업은 검증 가능한 완료 단위로 나눈다. 나눴다는 이유로 재승인받지 않는다.

## 2. 위험 등급과 승인

파일 수나 라벨보다 실제 영향을 기준으로 정하며, 여러 등급에 걸치면 높은 등급을 적용한다.
risk:A/B/C와 design-change 라벨은 분류 표시이며 승인·검토 증거가 아니다.

| 등급 | 예 | 실행·병합 조건 |
|---|---|---|
| A | 승인된 규약 안의 일반 구현, 문구·표시, 문서의 확인된 사실 갱신 | 관련 검사와 3절 조건을 만족하면 자동 병합 |
| B | 좌표·시간·캐시·밀도·상태 계산, 데이터 처리, API 연결, 배포·CI 변경, 테스트 기대값 변경, 운영 지침 변경 | 카드 후 구현을 계속한다. 현재 변경본의 독립 검토와 관련 검사 통과 후 자동 병합 |
| C | 제품·API·계산 의미·저장 데이터·스택 변경, 새 비용, 권한 확대·검사 약화, 포트·SSH·UFW 변경, 복구 불가능한 삭제 | 대상·영향·복구 방법을 구체화해 필요한 결정을 한 번 승인받는다. 승인 범위의 실행은 계속하고 B의 검토·병합 조건도 적용 |

- B·C 또는 반나절 이상 작업의 카드는 목표 / 관련 설계 절 / 완료 조건 / 변경 범위 / 증거 / 미완료 조건의 여섯 줄이다. 반복 계획 승인 단계는 두지 않는다.
- 설계의 빈칸·모순은 해당 문장과 최소 대안을 제시한다. 영향을 받는 결정만 보류하고 독립적으로 가능한 승인된 작업은 진행한다.
- 기존 스택 안의 함수·파일 분리, 일반 개발 의존성 선택은 담당 AI가 정한다. 새 의존성은 이유와 고정 버전을 남기고 데이터·보안·배포에 영향을 주면 B로 검토한다. 새 서비스·스택·비용은 C다.
- 이미 승인된 파일 삭제·데이터 교체도 승인된 대상과 복구 조건 안에서 실행한다. 병합된 작업 브랜치 정리는 자동이다.

## 3. Git·PR·자동 병합

작업 브랜치 → 구현·관련 검사 → commit → push → PR → 필요한 독립 검토 → squash merge → 병합된 브랜치 정리 순서로 수행한다.

**자동 병합은 다음을 모두 실제로 확인한 뒤에만 실행한다.**

1. main에 PR 경유와 필요한 CI 검사가 강제되고, 관리자 우회·force push·브랜치 삭제가 허용되지 않는다. 검사 결과의 출처도 확인한다.
2. 현재 변경본의 필요한 검사가 완료되어 성공했다. 누락·실패·취소·미실행·예상치 못한 skip은 통과가 아니다. 최신 main과의 통합 검사도 필요하다.
3. B·C는 검토한 기준/대상 커밋, 검토 담당, 검사와 미실행 검사, 중요한 지적 해결 근거가 있다. C는 해당 변경을 포함하는 사용자 승인 기록도 있다.
4. 검토 후 변경되면 변경분을 다시 검토한다. 병합 요청은 확인한 PR HEAD에 묶고, 준비되지 않은 PR은 draft로 유지한다.
5. main 직접 push, force push, 보호 규칙 우회, --admin 병합은 하지 않는다. 명령 실행 후 실제 병합·브랜치 삭제 결과를 원격에서 확인한다.

초기 전환 절차는 docs/automation-bootstrap.md에 있다. **CI 파일 추가만으로 보호 규칙이나 자동 병합이 켜지지 않는다.**
초기 PR에도 검사와 독립 검토가 필요하다. 보호 설정은 관리 권한이 있는 환경에서 수행한다.

## 4. 독립 검토와 검사의 범위

- 기본 작성자는 Claude Code, 중요한 변경의 검토자는 Codex다. Codex가 작성하면 Claude Code 등 다른 담당이 독립 검토한다. 작성자의 자기 검토는 독립 검토로 기록하지 않는다.
- 검토자는 저장소와 고정된 diff, 요구사항, 검사 증거를 직접 확인한다. 입력·기대 결과·실제 결과 또는 코드 근거가 있는 지적에 집중한다. 스타일 취향이나 요청 밖 확장은 제외한다.
- 같은 폴더를 두 에이전트가 동시에 수정하지 않는다. 검토 중 수정이 필요하면 담당을 넘기고 달라진 범위를 다시 확인한다.
- 확장 설치와 자동 연결 성공을 구분한다. 실제 호출과 결과 전달을 확인하기 전에는 사용자 개입 없이 독립 검토가 연결되었다고 보고하지 않는다. 연결 미완료면 A만 자동화하고 B·C는 검토 완료까지 draft로 남긴다.
- CI는 기록 가능한 환경에서 검사를 반복 실행한다. 작성자가 테스트·워크플로를 변경할 수 있으므로 그 자체가 독립적인 정답 증명은 아니다.
- 기대값은 규약·손계산 예제·검수한 자료에서 정한다. 구현 출력을 그대로 정답으로 삼거나 통과시키려고 기대값을 고치거나 검사를 끄지 않는다. 정당한 기대값 정정은 이유와 반례를 남기고 B 검토한다.
- 모의 OSRM·합성 데이터 검사와 실제 OSRM·실데이터·부하·한국 내 응답시간 검사를 구분한다. 미확인은 통과가 아니다. 게이트 일정 지연도 완료로 바꾸지 않는다.
- 제품 코드가 생기는 PR에 Python 검사, 타입·빌드, 계약·계산·로그 검사를 함께 추가한다. 현재 최소 CI로 제품의 정확성을 검증했다고 주장하지 않는다.

## 5. 배포·데이터·권한

- 일반 코드 배포는 검증된 워크플로, 승인된 대상, main의 검사된 커밋, 배포 후 실제 동작 확인, 복구 조건이 갖춰진 뒤 조건부 자동 실행한다.
- 데이터 교체는 B의 검수 기록과 현재 확정설계 5절의 새 버전 업로드·정지·참조 변경·재기동(필요 시 재생성)·5좌표 검사·직전 버전 보존을 따른다. 코드 배포와 분리한다.
- 롤백은 현재 데이터와 호환되는 직전 정상 이미지·설정으로 복구하고 실제 응답을 확인한다. 데이터 삭제나 비호환 역변환은 일반 코드 롤백으로 취급하지 않는다.
- 앱 /opt/geoleobom과 데이터 /srv/geoleobom/data는 공존할 수 있다. 경로 이전은 배포에 필요한 때 결정한다. 릴리스 방식에서는 Compose 프로젝트 이름, 영속 볼륨, 실제 마운트와 이미지 복구를 검증한다.
- root 접속 차단은 대체 관리 계정의 새로운 로그인·관리 권한 확인 뒤에 한다. 배포 키의 제한뿐 아니라 실행 스크립트·입력·Compose 파일의 수정 권한도 검토한다.
- 비밀값은 사용자가 지정된 비밀값 저장소에 입력한다. AI는 변수 이름과 설정 여부를 확인하며 값을 저장소·채팅·로그에 노출하지 않는다.
- 사용자 좌표 원문·검색어를 로그에 남기지 않는다. 큰 GeoPackage·OSRM·PBF 파일을 Git에 넣지 않는다.
- 도구의 허용·금지 설정은 명령별로 구성하고 실제 적용 여부를 확인한다. 광범위한 셸 허용이나 프로젝트 설정 파일만으로 서버·GitHub 권한 경계까지 강제됐다고 주장하지 않는다.

## 6. 보고와 문서

- PR 본문: 변경 이유·결과 / 검사와 실제 증거 / 위험 등급·독립 검토 / 미확인·필요한 결정 / 복구 방법(B·C).
- 채팅: 결과, 검증, 남은 일의 세 줄 안팎과 PR 링크. 승인된 Git 단계마다 재승인받지 않는다.
- STATUS.md: 운영 변화, 주간 정리, 막힘, 작업 중단·담당 인계 때 갱신한다. 현재 작업과 다음 행동을 짧게 남긴다.
- 브랜치·SHA 상태 표를 상시 동기화하지 않는다. 배포·검토·장애·인계에서 재현에 필요한 정확한 버전 식별자는 기록한다.
- README.md는 실행·검사·배포·복구 안내, PROJECT.md는 승인된 결정과 게이트 기록이다. 사실 갱신은 원인이 된 코드 PR에 포함한다. 필요한 단독 문서 PR도 허용한다.
- 현재 확정설계와 가이드 v1·이전 확정설계를 중복 복제하거나 경로를 임의로 옮기지 않는다. 이전 버전(v2.2·v2.1)은 이력 대조 때만 읽는다.

## 7. 막혔을 때

같은 실패가 두 번 반복되거나 새 증거 없이 수정만 반복되면 다음 순서를 따른다.

1. 현재 상태와 실패 로그 보존. 관계없는 업그레이드·재설치 중단.
2. 재현 입력과 기대·실제 결과를 하나로 고정.
3. 브라우저 → API → 데이터 조회 → OSRM의 경계를 확인.
4. 유력한 가설 하나와 예상 관찰 결과를 정해 검사.
5. 최소 수정 후 원래 실패와 관련 동작 재검사. 실행하지 못한 것은 미확인으로 인계.

## 8. 개발 환경

사용자 PC의 환경 결정은 PROJECT.md 3절을 따른다. Windows·PowerShell, UTF-8(BOM 없음), Git 저장은 LF다. WSL의 Git·Node·Python을 번갈아 쓰지 않는다.
GitHub CI와 별도 검토 환경에서의 Linux 실행은 사용자 PC 환경을 바꾼 것이 아니다. 실제 검사 환경을 구분해 보고한다.
