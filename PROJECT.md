# 걸어봄 — 프로젝트 안내 (PROJECT.md)

이 파일은 설계 문서가 아니다. **기준 문서를 찾아가는 안내**와 **승인된 결정**, 그리고 **게이트 통과 확인 기록**만 둔다.
제품 범위·기술 스택·계산 규약·API 계약은 **현재 확정설계 v2.3 원문이 유일한 규약 원문**이다. 이 파일은 규약을 새로 정하지 않고, 원문을 복제하지도 않는다.
현재 확정설계 10절 게이트가 "PROJECT.md에 기록"을 요구하는 항목만 **요약 형태로 확인 기록**을 남긴다(8절). 그 기록과 원문이 다르면 **원문이 맞다.**

## 1. 기준 문서

| 문서 | 경로 | 역할 | 고칠 수 있는 사람 |
|---|---|---|---|
| **확정 설계 v2.3** | `docs/걸어봄_확정설계_v2.3.md` | 동결된 제품·계산·API·배포 규약. **현재 확정설계 — 단일 기준.** v2.2에 API 표현 의미 4건만 확정 | 본인 승인 후 문서 먼저 개정 |
| 확정 설계 v2.2 | `docs/걸어봄_확정설계_v2.2.md` | **동결 이력** — 수정하지 않는다. v2.2→v2.3 차이는 v2.3 부록 E | 수정 없음 |
| 확정 설계 v2.1 | `docs/걸어봄_확정설계_v2.1.md` | **이전 동결 이력** — 수정하지 않는다. v2.1→v2.2 차이는 v2.2 부록 D | 수정 없음 |
| 개발 운영 가이드 v1 | `docs/걸어봄_개발운영가이드_v1.md` | 검증·학습 참고. 현재 승인·보고·갱신 규칙은 AGENTS.md | 본인 |
| PROJECT.md (이 파일) | `PROJECT.md` | 안내 + 승인된 결정 | 본인 |

현재 확정설계와 다른 구현은 **문서를 먼저 개정한 뒤** 진행한다.

## 2. 현재 확정설계 어디를 볼 것인가

무엇을 확인하려는지에 따라 아래 절을 연다. 내용을 이 파일에 옮겨 적지 않는다.

| 알고 싶은 것 | v2.3 절 |
|---|---|
| 이 제품이 무엇이고 어디까지 만드는가 | 3. 최종 제품 범위 |
| 어떤 기술을 쓰는가 | 4-1. 스택 |
| 좌표 순서·반올림·시간 계수·버전 규칙 | 4-2. 좌표·시간·버전 규약 |
| 분석이 어떤 순서로 계산되는가 | 4-3. 분석 알고리즘 |
| API 주소와 응답 필드·에러 코드 | 4-4. API 계약 |
| 화면에 상태를 어떻게 표시하는가 | 4-5. 프론트 표시 규칙 |
| 서버 구성·포트·로그·데이터 교체 절차 | 5. 서버 구성 |
| 무엇을 먼저 만들고 무엇을 버리는가 | 6. Must / Should / Could / Won't |
| 누가 무엇을 맡는가 | 7. 3명 역할 |
| 몇 주차에 무엇을 끝내는가 | 8. 14주 일정 |
| 지금 통과해야 할 확인 항목 | 10. 구현 시작 전 게이트 |
| 구현 규약 한눈에 보기 | 부록 B |
| 데이터 파일 사양·생성 절차 | 부록 C |
| 계약 서버 사양, 해외 리전 검증·실패 대응 | 1-8, 10. 게이트 2 |
| v2.1에서 무엇이 바뀌었나 | 부록 D |

현재 작업 방식과 승인 경계는 `AGENTS.md`에서 확인한다. 아래 가이드 절은 검증·학습 배경을 찾는 안내다.

| 알고 싶은 것 | 가이드 절 |
|---|---|
| 도구·역할 배치, 개발 환경 선택 원칙 | 2 |
| 저장소에 둘 파일과 각 역할 | 3 |
| 작업 한 바퀴(카드 → 계획 → 구현 → 검토 → 확인) | 4 |
| 검증의 정답을 만드는 방법 | 5 |
| 교차 검증이 필요한 변경 유형 | 6 |
| Git·배포·권한·비밀키 운용 | 7 |
| 막혔을 때의 순서 | 8 |

## 3. 승인된 개발환경 결정

**이 절은 확정설계 규약이 아니다.** 확정설계는 제품·계산·서버 규약을 정하고, 이 절은 **내 PC에서 개발할 환경**을 정한다.
근거는 개발 운영 가이드 2절("기존에 잘 동작하는 개발 환경까지 무조건 바꾸지 않는다", "Windows·WSL의 Python이나 Git을 번갈아 사용하지 않는다")이다.
결정을 바꾸려면 이 절을 고치고 STATUS.md에 기록한다.

| 항목 | 결정 | 이유 | 상태 |
|---|---|---|---|
| Git·Node·Python 위치 | **Windows 하나로 통일** | 두 환경을 번갈아 쓰지 않는다. 현재 Windows 도구가 모두 정상 동작 | 승인됨 (2026-09-10) |
| WSL2 | **직접 사용하지 않음.** Docker Desktop을 설치할 때 Docker 엔진 용도로만 쓴다 | Windows 11 Home은 Docker가 WSL2 방식으로만 동작. 별도 Ubuntu 개발환경은 만들지 않음 | 승인됨 (2026-09-10), **아직 설치하지 않음** |
| Docker Desktop | Week 1 말 ~ Week 2 초에 설치 | OSRM 전처리와 로컬 OSRM 실행에 필요. Week 1의 VPS·HTTPS 작업에는 불필요 | 승인됨 (2026-09-10), 설치 미실행 |
| 프로젝트 Python | **3.12로 고정.** 기존 3.14.4는 지우지 않고 그대로 둠 | GeoPandas 계열의 Windows 설치 안정성, 서버(Ubuntu 24.04) 기본 버전과 일치 | 승인됨 (2026-09-10). **3.12.10 설치 완료 (2026-09-11), 3.14.4 유지** |
| Node | **24.14.1 그대로 사용** | 장기 지원(LTS) 버전. 버전 관리 도구는 두지 않음 | 승인됨 (2026-09-10) |
| 줄바꿈 | 텍스트 파일은 저장소에 **LF**로 저장 | Windows에서 만든 셸·설정 파일이 Linux 서버에서 깨지지 않게 함. `.gitattributes`가 담당 | 승인됨 (2026-09-10) |

**바꿀 조건**: Windows에서 GeoPandas 설치가 두 번 연속 실패하거나, 전국 OSRM 전처리 속도가 작업을 막을 정도이면 **데이터 생성 작업만** WSL2로 옮긴다. 그 경우에도 앱 코드 개발은 Windows에 남긴다.

## 4. 확인된 PC 환경 (2026-09-10, 읽기 전용 확인)

| 항목 | 값 |
|---|---|
| OS | Windows 11 Home, 빌드 26200 (64비트) |
| CPU | AMD Ryzen 5 9600X (6코어 12스레드) |
| 메모리 | 31.6GB |
| 디스크 여유 (C:) | 약 598GB |
| Git | 2.53.0 — `C:\Program Files\Git\cmd\git.exe` |
| Node | 24.14.1 — `C:\Program Files\nodejs\node.exe` |
| npm | 11.11.0 |
| Python | 3.14.4 — `C:\Users\sdsdo\AppData\Local\Python\pythoncore-3.14-64\python.exe` |
| pip | 26.0.1 |
| Docker | 미설치 |
| WSL | 미설치 (가상화 기능은 사용 가능) |
| SSH 클라이언트 | 있음 — `C:\WINDOWS\System32\OpenSSH\ssh.exe` |
| VS Code 확장 | Claude Code, ChatGPT(Codex), Python |

## 5. 저장소 구성

| 경로 | 역할 |
|---|---|
| `docs/` | 기준 문서: 확정 설계 v2.3(현재 기준), v2.2·v2.1(동결 이력), 개발 운영 가이드 v1 |
| `PROJECT.md` | 이 파일 |
| `AGENTS.md` | 두 AI가 따를 공통 작업 규칙 |
| `CLAUDE.md` | `AGENTS.md`를 가져오는 연결 파일 |
| `README.md` | 실행·검사·배포·복구 방법 (확인된 것만) |
| `STATUS.md` | 현재 상태와 다음 작업 하나 |
| `.gitattributes` | 줄바꿈 규칙 |
| `.gitignore` | 저장소에서 제외할 파일 |

2026-09-11 운영 전환 승인에 따라 `api/`(FastAPI), `web/`(React·TS·Vite), `data/`(생성 스크립트·작은 픽스처), `deploy/`(배포 설정)를 사용한다. 테스트는 각 패키지 안에 둔다. `.github/`는 CI·PR 설정이다. 제품 디렉터리는 해당 구현을 시작하는 PR에서 만들며 별도 폴더 생성 승인은 반복하지 않는다.

## 6. 승인 경계

현재 등급·병합·배포 조건은 `AGENTS.md` 2~5절에 둔다. 이 절에 권한 매트릭스를 복제하지 않는다.

2026-09-11 사용자 승인: 커밋·작업 브랜치 push·PR·조건부 squash merge·병합 브랜치 정리는 AI가 수행한다. C급 결정은 대상과 복구 방법을 확인해 한 번 승인받고, 승인된 범위의 실행에는 같은 승인을 반복하지 않는다. 초기 보호 설정·최소 CI부터 검증하고 제품 골격·배포 자동화는 다음 완료 단위로 진행한다.

API·계산 의미·저장 데이터·스택·동결된 서버 규약 변경은 승인 후 현재 확정설계 문서를 먼저 개정한다. 2026-09-11 운영 전환은 그 제품·기술 규약을 바꾸지 않았다.

**2026-09-11 C급 승인 — API 표현 공백 4건 확정.** 사용자 승인 문구: "4개 결정 승인, v2.3 진행". 대상은 제품 오류 응답 body와 HTTP 매핑, `density.count`의 상태별 의미, `nearest[].best`·`top3`의 상태별 표현, `computed_at`의 시각 의미다. **상세 규약은 v2.3 4-3·4-4·4-5·부록 B·부록 E를 본다. 이 파일에 복제하지 않는다.** 제품 범위·스택·계산 모델·성능 목표는 바뀌지 않았다.

앱 `/opt/geoleobom`과 설계의 데이터 `/srv/geoleobom/data` 경로는 공존 가능하다. 경로 이동·GHCR 배포 구현·SSH 변경은 이번 초기 CI 작업에 포함하지 않는다.

## 7. 미완료 · 확인 대기

- **게이트 1의 규약 고정 항목**: v2.2 10절이 요구하는 "좌표 순서·5자리 규약, 캐시 키 구성, k 계수·`time_model_version`, 밀도 집계 상태, 최근접 상태 4종"의 PROJECT.md 기록은 **완료 (2026-09-11).** 기록 위치는 8절이다.
- **v2.1·v2.2 4-3 설명 정정 메모** (개발 운영 가이드 14절): v2.1 4-3(v2.2 4-3도 같은 문장)의 "`sources`/`destinations`를 생략하면 161×161 행렬이 되어 `--max-table-size 200`을 초과한다"는 **설명만 산술상 잘못됐다**(161² < 200²). **제품·아키텍처·구현 규약 변경은 아니다.** `--max-table-size 200`, `sources=0`·`destinations` 명시, FastAPI 목적지 ≤160 가드, 실제 1×160 응답 검사는 **그대로 유지한다.** **v2.1·v2.2 원문은 수정하지 않는다.** v2.2는 서버 관련 변경만 담았으므로 이 문장도 그대로 남아 있다. 이 메모가 해당 설명의 기록이다.
- Git 저장소 초기화, Git 사용자 이름·이메일 설정: **완료.** 작성자 정보는 이 저장소 `--local` 설정에만 등록했다.
- Docker Desktop 설치: 미완료. **Python 3.12.10 설치: 완료 (2026-09-11). 기존 3.14.4는 그대로 둠.**

## 8. 게이트 1 — API 계약 규약 고정 확인 (2026-09-11)

현재 확정설계 10절 게이트 1의 항목 **"API 계약(4-4)에 좌표 순서·5자리 규약, 캐시 키 구성, k 계수·`time_model_version`, 밀도 집계 상태, 최근접 상태 4종을 고정하고 PROJECT.md에 기록"** 에 대한 확인 기록이다.

**이 절은 규약을 새로 정하지 않는다.** 아래 값은 모두 확정설계(4-2·4-3·4-4)에서 이미 동결된 것이며, 게이트가 요구한 항목만 원문과 대조해 옮겨 적었다. **4-4의 전체 필드 표는 복제하지 않는다.** 규약 원문은 v2.3이며, 이 값들은 v2.2에서 v2.3으로 바뀌지 않았다.

### 8-1. 좌표 (4-2)

| 항목 | 고정된 값 |
|---|---|
| 내부 좌표 순서 | **`[lon, lat]`** — API·계산·캐시 키·GeoPackage 조회 전부 동일 |
| 반올림 시점 | 검색·핀 입력 좌표를 **최초 입력 시 한 번만** 소수 **5자리**로 반올림. 이후 단계에서 다시 반올림하지 않는다 |
| 경계 변환 | URL `/p/{lat},{lng}`와 카카오 SDK `(lat, lng)`는 **입출력 경계에서 명시적으로 변환**한다 |

### 8-2. 캐시 키 (4-3)

| 항목 | 고정된 값 |
|---|---|
| 키 구성 | **`analyze:{data_version}:{time_model_version}:{lon}:{lat}`** |
| 좌표 표기 | **계산에 사용한 5자리 문자열을 그대로** 쓴다. 격자 반올림 없음 |

### 8-3. 시간 모델과 버전 (4-2)

| 항목 | 고정된 값 |
|---|---|
| 시간 계산 | **`service_seconds = osrm_duration_seconds × k`, `k = 5.0 / 4.5`** |
| `time_model_version` | **프로필 또는 k가 바뀌면 값을 올린다.** 응답과 캐시 키에 항상 포함 |

### 8-4. 상태 값 (4-3·4-4)

| 구분 | 고정된 값 |
|---|---|
| 밀도 집계 상태 | **`complete` / `capped` / `incomplete`** |
| `capped` 의미 | 20곳 도달 시 즉시 종료하고 **`20+`** 로 표시 |
| `incomplete` 의미 | 집계 미완료. **절대 0으로 대체하지 않는다** |
| 최근접 상태 4종 | **`ok` / `uncertain` / `unreachable` / `none`** |

### 8-5. 이름 변경 경계

**API 필드명과 에러 코드는 현재 확정설계 4-4가 기준이다.** 이름을 바꾸는 것은 **문서 개정 사항**이며(6절 승인 경계), 문서를 먼저 고친 뒤에만 구현에 반영한다. 이 파일에 필드 목록을 다시 적지 않는다. 상태별 값 표현(`best`·`top3`·`count`·`computed_at`)과 오류 body는 v2.3에서 확정됐다.

### 8-6. 이 기록의 한계

이 절은 **문서 기록 항목 하나**를 통과시킨 것이다. 위 규약이 **코드에서 실제로 지켜지는지는 아직 검사하지 않았다** (제품 코드 없음). 실제 동작 확인은 게이트 2의 캐시 분리·10분 경계·밀도·최근접 검사 항목에서 수행한다.
